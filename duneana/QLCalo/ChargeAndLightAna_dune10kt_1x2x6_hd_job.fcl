#include "ChargeAndLightAna.fcl"
#include "opticaldetectormodules_dune.fcl"
#include "opticaldetectorservices_dune.fcl"
#include "services_dune.fcl"
#include "tools_dune.fcl"


process_name: CaLo

services:
{
  @table::dunefd_services
  TFileService: { fileName: "charge_light_hist.root" }
  TimeTracker:       {}
  RandomNumberGenerator: {} #ART native random number generator
  AuxDetGeometry: @local::dune10kt_1x2x6_auxdet_geo
  Geometry:              @local::dune10kt_1x2x6_v5_refactored_geo
}

source:
{
  module_type: RootInput
  maxEvents:  -1      # Number of events to create
  #specify from command line with -s or --source
   
}


physics:
{
   
   analyzers:
   {
     calana:  @local::standard_ChargeAndLight
   }
   
   
   analyzeIt:  [ calana ]
   
   #define the output stream, there could be more than one if using filters 
   stream1:  [ out1 ]
   
   #end_paths is a keyword and contains the paths that do not modify the art::Event, 
   #ie analyzers and output streams.  these all run simultaneously
   end_paths:     [analyzeIt, stream1]  
}

outputs:
{
   out1:
   {
      module_type: RootOutput
      fileName:    "charge_light_gen.root"
      #default file name, can override from command line with -o or --output
   }

}
